---
export interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, body } = Astro.props as Props;
---
<ul>
	<li>
		<a href={href}>{title}: {body}</a>
	</li>
</ul>

<style>

</style>
